# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    # Rolling is broken with current nixpkgs-unstable
    # url: github:cachix/devenv-nixpkgs/rolling
    url: github:NixOS/nixpkgs/nixpkgs-unstable

imports:
  - ./common
